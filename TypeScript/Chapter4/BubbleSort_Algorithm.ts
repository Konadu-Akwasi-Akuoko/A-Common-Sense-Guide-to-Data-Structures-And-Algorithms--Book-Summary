function BubbleSort(arr: number[]) {
  let unsortedUntilIndex = arr.length - 1;
  let sorted = false;
  while (!sorted) {
    sorted = true;
    arr.forEach((item, index, arr) => {
        if(item > arr[index + 1]){
            
        }
    })
  }
}
